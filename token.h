enum Token{
    IF,
    THEN,
    DO,
    WHILE,
    FOR,
    ELSE,
    SWITCH,
    ID,
    NUM,
    SOMA,
    INT,
    BOOL,
    ATRIBUICAO
};

void e_digito();
void e_caracter();
void e_operador();
